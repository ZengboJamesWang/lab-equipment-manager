# API URL (optional - defaults to /api with proxy)
# Leave commented for production (uses /api)
# For local development without proxy, uncomment and set to: http://localhost:5000/api
# VITE_API_URL=/api

# Site name (displayed in header and login page)
VITE_SITE_NAME=Lab Manager
